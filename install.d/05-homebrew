if _dot_is_osx; then
    if _dot_on_path brew; then
        for f in coreutils docker fd findutils git gnu-sed gnu-tar gnupg grep hub jq ripgrep tree vim watch zsh; do
            brew install $f
        done
    else
        echo "WARNING: HomeBrew not installed"
    fi
fi
